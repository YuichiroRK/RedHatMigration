services:
  migration-tool:
    build: .
    container_name: redhat_migration_tool
    ports:
      - "8000:8000"
    volumes:
      - ./migraciones.db:/app/migraciones.db
      - ./data:/app/data
    restart: always